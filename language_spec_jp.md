# Flap Language Specification

Flapはミニマルかつ強力なスタックベースの難解プログラミング言語（Esolang）です。

## 1. データ型

Flapは唯一のデータ型として **64bit符号付き整数 (`i64`)** を扱います。
文字列（String）も整数（ASCIIコード）のシーケンスとしてスタック上で表現されます。

- **Character**: ASCII文字（内部的には整数として保持）

## 2. 構文規則

### 2.1 トークナイズ

- **数値**: 連続する数字は1つの整数として解釈されます。
- **コマンド**: 数字以外の非空白文字（いくつかの例外を除く）は、それぞれ1文字のコマンドとして解釈されます。
- **区切り文字 (`,`)**: 数値と数値、または数値とコマンドを明示的に区切りたい場合に使用します。
  - 例: `10,20+` (10をプッシュ、20をプッシュ、加算)
- **空白**: 半角スペース、タブ、改行は単なる区切りとして無視されます。

### 2.2 コメント

- `=` で囲まれた範囲はコメントとして扱われ、実行時に無視されます。
  - 例: `10 = ここはコメント = 20 +`

---

## 3. コマンド一覧

### 3.1 算術演算

上位2つの値をポップし、計算結果をプッシュします。

| コマンド | 名前 | 説明                      | スタック変化 (a, b -> Result) |
| :------: | :--- | :------------------------ | :---------------------------- |
|   `+`    | Add  | 加算                      | `a, b -> a + b`               |
|   `-`    | Sub  | 減算                      | `a, b -> a - b`               |
|   `*`    | Mul  | 乗算                      | `a, b -> a * b`               |
|   `/`    | Div  | 除算 (0除算は0をプッシュ) | `a, b -> a / b`               |
|   `%`    | Mod  | 剰余 (0除算は0をプッシュ) | `a, b -> a % b`               |

### 3.2 スタック操作

スタックの状態を直接操作します。

| コマンド | 名前 | 説明                  | スタック変化             |
| :------: | :--- | :-------------------- | :----------------------- |
|   `:`    | Dup  | スタックトップを複製  | `a -> a, a`              |
|   `;`    | Pop  | スタックトップを破棄  | `a -> (なし)`            |
|   `x`    | Swap | 上位2つの値を入れ替え | `a, b -> b, a`           |
|   `@`    | Rot  | 上位3つを回転         | `a, b, c -> b, c, a`     |
|   `R`    | Rev  | スタック全体を反転    | `[1, 2, 3] -> [3, 2, 1]` |

### 3.3 入出力

| コマンド | 名前        | 説明                                                                                                                                               | スタック変化        |
| :------: | :---------- | :------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------ |
|   `r`    | Read Num    | 標準入力から空白区切りの数値を読み取り、スタックにプッシュします。数値でない場合はエラーになります。**EOF(入力終了)の場合は0がプッシュされます。** | `-> n`              |
|   `t`    | Create Char | 標準入力から1文字読み取り、そのASCIIコードをスタックにプッシュします。**EOF(入力終了)の場合は0がプッシュされます。**                               | `-> c`              |
|   `T`    | Text        | 標準入力から1行読み取り、各文字のコードをスタックにプッシュします。**最後の文字がスタックトップに来ます。**                                        | `-> c1, c2, ... cn` |
|   `p`    | Print       | 数値をポップして標準出力に表示                                                                                                                     | `n -> (なし)`       |
|   `P`    | PChar       | 文字コードをポップして文字として表示                                                                                                               | `n -> (なし)`       |

---

## 4. 制御構文

スタックの状態に基づいて実行フローを制御します。

### 4.1 繰り返し (`[` ... `]`)

- **Whileループ**
- `[` に到達したとき、スタックトップの値が **0以外** なら内部を実行します。
- `]` に到達したとき、スタックトップの値が **0以外** なら `[` の直後へジャンプします。
- **注意**: ループの開始時・終了時にトップの値は **ポップされません**。

### 4.2 条件分岐 (`(` ... `)`)

- **If文**
- `(` に到達したとき、スタックトップの値が **0以外** なら内部を実行します。
- `(` に到達したとき、スタックトップの値が **0** なら対応する `)` までジャンプします。
- **重要**: 内部が実行されたかどうかにかかわらず、ジャンプまたは正常終了した時点で**条件値は必ずポップされます**。

---

## 6. 制限事項

- 現在のバージョンでは **ASCII文字 (0-255)** のみをサポートしています。
- マルチバイト文字（日本語など）の入力や出力は正しく動作しません。

---
